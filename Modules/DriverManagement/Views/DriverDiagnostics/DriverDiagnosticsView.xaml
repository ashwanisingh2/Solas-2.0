<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Modules.DriverManagement.Views.DriverDiagnostics.DriverDiagnosticsView">
    <Grid Margin="12">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Analyze Logs" Command="{Binding AnalyzeCommand}" Width="120" />
            <Button Content="Refresh History" Command="{Binding RefreshHistoryCommand}" Width="120" Margin="8,0,0,0" />
        </StackPanel>

        <DataGrid ItemsSource="{Binding Diagnostics}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding EventDate}" Width="150" />
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="80" />
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="200" />
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>